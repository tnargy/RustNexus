listen_addr            = "0.0.0.0:8080"
database_path          = "./data/metrics.db"
offline_threshold_secs = 120   # 2 minutes
retention_days         = 30
log_level              = "info"
hmac_secret            = ""          # set to a non-empty string to enable HMAC-SHA256 request verification
